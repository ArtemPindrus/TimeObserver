<UserControl
    x:Class="TimeObserver.Views.AddReminder.AddOneTimeReminderView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:TimeObserver.Views.AddReminder"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="200"
    d:DesignWidth="200"
    mc:Ignorable="d">
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Trigger At: " />
            <TextBox Width="50" Text="{Binding Time, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <Button
            Margin="0,5"
            HorizontalAlignment="Left"
            Style="{StaticResource AddReminderButton}" />
    </StackPanel>
</UserControl>
